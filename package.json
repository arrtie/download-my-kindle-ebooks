{
  "name": "download-kindle-books",
  "version": "1.0.0",
  "description": "download kindle books",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node scripts/bundle.js",
     "watch": "node scripts/bundle.js --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  }
}
